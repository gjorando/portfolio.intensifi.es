# [portfolio.intensifi.es](portfolio.intensifi.es)

## Install

```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Then, run `pelican -rl` to run the development server.